# Native FFI docs discovery / Native FFI 文档发现

## 中文

- 为 ABI 协议和迁移指南补充 `calcit docs read/search` 可索引的 frontmatter。
- 增加常用 symbol、生命周期和迁移术语的 aliases/entry points。
- 本次只改文档索引，不改变已发布 ABI、常量或运行时行为。

## English

- Add frontmatter that makes the ABI protocol and migration guide discoverable through `calcit docs read/search`.
- Index common symbols, lifecycle concepts, and migration terms as aliases and entry points.
- This documentation-only change does not alter any published ABI, constant, or runtime behavior.
